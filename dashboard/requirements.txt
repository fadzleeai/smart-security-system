# requirements.txt — STREAMLIT CLOUD SIDE
# This is what Streamlit Community Cloud installs when it deploys your app.
#
# CHANGED vs original: added `requests` — needed by data_source.py to call
# your Pi's API over the Cloudflare Tunnel URL.

streamlit>=1.35.0
pandas>=2.0.0
plotly>=5.18.0
requests>=2.31.0

# ── No longer needed on the cloud side ────────────────────────────────────────
# paho-mqtt, psutil, opencv-python, face_recognition, RPi.GPIO
# — these stay on the PI ONLY (used by your detection backend, not by the
#   cloud dashboard). Do not add them here, they would just slow down
#   Streamlit Cloud's deploy and aren't used by data_source.py anymore.
